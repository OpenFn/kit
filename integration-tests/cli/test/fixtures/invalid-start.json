{
  "options": {
    "start": "nope"
  },
  "workflow": {
    "steps": [
      {
        "id": "x1",
        "expression": "fn((state) => state)"
      },
      {
        "id": "x2",
        "expression": "fn((state) => state)"
      }
    ]
  }
}
