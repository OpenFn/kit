// https://github.com/OpenFn/kit/issues/251
const createAutoInstall = (options) => {
  return async (plan) => {
    // find all adaptors
    // auto install what we need
    // return when ready
  };
};

export default createAutoInstall;
