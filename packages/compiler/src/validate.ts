/**
 * Validate an AST to ensure it's a valid job
 * Returns a list of errors and warnings
 * 
 * Errors:
 * - non-open fn imports
 * - any code that will be removed (generally top-level stuff)
 * 
 * Warnings:
 * - no adaptor import ?
 */

export default function validateJob() {

}