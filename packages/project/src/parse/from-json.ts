// this function has the job of taking an abitrary chunk of JSON
// and working out if it's state v1 or project v2 file
export default (
  state: JSON // well it'll be state or project won't it
  // I don't think we ever need options here?
) => {
  // if this looks like state v1, return from app state
  // if htis looks like state v2
};
